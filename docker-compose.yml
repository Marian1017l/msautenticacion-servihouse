version: "3.8"
services:
  auth:
    build: .
    container_name: msauthenticationservihouse
    ports:
      - "8001:8001"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    networks:
      - gateway-network

networks:
  gateway-network:
    external: true